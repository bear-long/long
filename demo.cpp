#include <opencv2/opencv.hpp>
#include <queue>
#include <mutex>
#include <condition_variable>
#include <Multithreading.h>
#include <HK_camera.h>
#include <Multithreading.h>
template <typename T>

int main() {
    // 定义每个队列的大小
    const size_t queueSize = 10;
    vector<MyCircularQueue> queue1(queueSize);
    vector<MyCircularQueue> queue2(queueSize);
    vector<MyCircularQueue> queue3(queueSize);

}

    return 0;
}
